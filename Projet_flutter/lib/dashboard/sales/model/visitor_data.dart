class VisitorData {
  final int month; // 0 to 11 (Jan to Dec)
  final double loyal;
  final double newCustomer;
  final double unique;

  VisitorData(this.month, this.loyal, this.newCustomer, this.unique);
}
